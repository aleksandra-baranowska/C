#include <stdio.h>

int main(void) 
{
    int zap³ata, suma, dni;

    printf("Podaj przepracowane dni: ");
    scanf("%d", &dni);

    zap³ata = 0;

    while (zap³ata++ < zakres) 
    {
        suma = suma + zap³ata;
    }

    printf("Suma za %d dni wynosi %d\n", dni, suma);
    return 0;
}
